# Python
__pycache__/
*.py[cod]
*.pyo
venv/
.venv/
*.egg-info/

# Runtime data — never commit these
data/
dashboard/static/crops/
pipeline.log
flask.log
dashboard.log
pipeline.pid
dashboard.pid

# Models — too large for git, download separately
models/

# Config with credentials — use config.example.json instead
config.json

# OS
.DS_Store
Thumbs.db